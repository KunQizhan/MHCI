# 实验前完成清单 / Pre-Experiment Checklist

---

## 1) 原型稳定性与效度检查（必须）

### ✅ 已修复：菜单中心 clamp 后方向判定错位（关键 bug）

**问题描述**：`showRadialMenu()` 在触点靠近屏幕边缘时会把菜单中心 clamp 到 `adjX/adjY`（存入 `state.menuCenter`），但 `onMMPointerMove()` 和 `onMMPointerUp()` 计算角度时仍使用原始 `state.touchStart`。

当 `touchStart` 和 `menuCenter` 不同（即靠近边缘时），视觉高亮的扇区 ≠ 实际判定的扇区，导致"我明明划向它了，但系统选了别的"。

**修改内容**（`index_fixed.html` 已完成，共 4 处改动）：

| 位置 | 改前 | 改后 |
|------|------|------|
| `onMMPointerMove` | `dx = cx - state.touchStart.x` | 拆分：expert-mode 距离检测仍用 `touchStart`；菜单可见后角度计算改用 `state.menuCenter` |
| `onMMPointerUp` | `dx = cx - state.touchStart.x` | 同上，用 `state.menuCenter` |
| `onMMPointerDown` | 无 menuCenter 重置 | 新增 `state.menuCenter = null` 防止残留上一次试次的值 |
| `showRadialMenu` 注释 | 原有 TODO 注释"update touch start"（未执行） | 改为说明已通过 menuCenter 解决 |

**验证方法**：在手机上，故意用手指靠近屏幕四个角落触发 Marking Menu，观察高亮扇区是否与手指滑动方向一致。对比修复前后，边缘区域的误选率应明显下降。

### ✅ 已修复：删除未实现的 hysteresisAngle

`CONFIG.hysteresisAngle = 8` 从未在角度判定逻辑中使用。原注释 "Hysteresis: only switch if new sector is different" 是误导——那只是"不切换到同一扇区"的基本检查，不是真正的角度滞回。

已删除该配置项，替换为注释说明原因。`sectorCrossings` 仍然被记录，可作为边界不稳定性的代理指标。

### ✅ 已修复：Baseline 按钮布局 3×3 → 4×2

原来 8 个按钮用 3 列排列（3+3+2），最后一行只有 2 个按钮，右下角空缺。改为 4 列（4+4），消除位置偏差。

### ✅ 已验证：Practice trials 不写入正式数据

`recordTrial()` 第 955 行：`if (!state.isPractice)` 才 push 到 `allTrialData`。**逻辑正确。**

此外，`state.globalTrialIdx` 也只在 `!isPractice` 时递增，所以正式数据的 trialIndex 从 0 开始连续编号，不受 practice 影响。

### ✅ 已验证：Trial 状态正确 reset

`presentTrial()` 做了以下重置：
- 清除所有 `.tap-btn` 的 activated/wrong class
- `ctx.clearRect` 清空 canvas
- 隐藏 `mm-preview` 和 `feedback-overlay`
- 调用 `overlay_clearLabels()` 清空扇区标签
- 重置 `state.gestureActive = false`
- 每次 `onMMPointerDown` 重置 `menuCenter`、`currentSector`、`sectorCrossings`

**没有发现叠加/卡死/残留问题。**

### ⚠️ 小风险点（不影响实验，但值得知道）

| 风险 | 影响 | 处理 |
|------|------|------|
| `sectorCenter()` 函数定义了但从未调用 | 无 | 保留不碍事，不用管 |
| `lastCommittedSector` 在 state 中初始化但从未读取 | 无 | 同上 |
| CSV 导出时先 filter 掉 `gesture*` 再加回 gestureLengthMm 等，靠 `new Set()` 去重 | 功能正常但代码不优雅 | 不影响数据正确性 |
| `feedbackDurationMs = 600ms` 期间触摸无法开始新试次 | 设计如此，是 feature 不是 bug | 保持原样 |

---

## 2) 实验执行方案（面向参与者，可直接复制发送）

以下内容你可以直接复制到微信/邮件/Notion 发给参与者：

---

### 📱 Marking Menu Study — 参与指南

**Hi！感谢你参加我的 Mobile HCI 课程实验。这个实验评估一种手机上的按钮激活方式，大约需要 15–20 分钟。**

#### 准备事项
- 用**手机**打开链接（Chrome 浏览器最佳，Safari 也可以）
- 请**竖屏**使用，不要横屏
- 找一个安静的地方**坐着做**，不要边走边做
- 打开**勿扰模式**（很重要！来电/通知会中断手势导致数据丢失）
- 不需要注册/登录任何东西

#### 操作流程
1. 打开链接后，在 **Participant ID** 处输入我分配给你的编号（如 P01）
2. 选择你的**惯用手**（左手/右手）
3. 选择我告诉你的 **Group 编号**（1 或 2 或 3）
4. 点 **Start Experiment**

你会依次完成 **3 个条件**，每个条件的流程是：
- 先看操作说明 → 点 Begin
- 完成 **8 次练习**（熟悉操作，不计入数据）
- 点按钮进入 **24 次正式试验**
- 最后填一份**简短问卷**（打分即可，1 分钟左右）

#### 三种操作方式
- **Direct Tap（直接点按）**：屏幕上有 8 个按钮，顶部显示目标名，直接点对应按钮
- **Marking Menu（圆形菜单）**：在空白区域**按住不动**→ 圆形菜单出现 → **滑向目标方向** → **松手确认**。如果不想选，松手前**回到中心**即可取消
- 两种 Marking Menu 区别只是**菜单半径不同**（小/大），操作方式相同

#### 完成后
3 个条件 + 3 份问卷全部完成后，会出现结果页面。请点击：
- **📥 Download All Data (JSON)** — 下载数据文件

然后把下载的文件 **发给我**（微信/邮件均可）。文件名类似 `study_data_P01.json`。

#### 隐私说明
实验只收集**匿名编号**（如 P01），**不收集姓名、学号或任何个人信息**。数据仅用于课程作业。

谢谢你！有问题随时联系我 🙏

---

## 3) 部署与发链接（2 套方案）

### 方案 A：GitHub Pages（推荐，最稳）

1. 创建 GitHub 账号（如果没有）
2. 新建 repository：`marking-menu-study`（选 Public）
3. 上传 `index.html`（就一个文件）：
   - 点 "Add file" → "Upload files" → 拖入 `index_fixed.html`，**重命名为 `index.html`**
   - 或者 "Add file" → "Create new file" → 文件名输入 `index.html` → 粘贴全部代码
4. 进入 Settings → Pages → Source 选 `main` branch → Save
5. 等 1–2 分钟，访问 `https://你的用户名.github.io/marking-menu-study/`
6. 把这个链接发给参与者

**优点**：免费、永久、全球 CDN
**注意**：如果代码有更新，push 后需等 1–2 分钟生效

### 方案 B：Glitch（最简单，即改即生效）

1. 打开 https://glitch.com → 注册/登录
2. New Project → `glitch-hello-website`
3. 左侧文件列表点 `index.html`，**全选删除**，粘贴你的完整代码
4. 可以删掉其他文件（style.css, script.js）因为你是单文件
5. 左上角点项目名 → 可以自定义名称如 `marking-menu-study`
6. 点 "Share" → 复制 Live site URL（类似 `https://marking-menu-study.glitch.me`）
7. 发给参与者

**优点**：即时生效，不需要 git
**注意**：免费版闲置 5 分钟后休眠，首次打开需等 10-15 秒唤醒

### 发链接前的最终测试清单

- [ ] 用**自己手机**完整走一遍所有 3 条件（不用做满 24 次，但至少验证流转正确）
- [ ] 确认数据下载能触发（CSV 和 JSON 都试一下）
- [ ] 确认边缘触发 Marking Menu 时高亮扇区与手指方向一致
- [ ] 用**另一部手机或让朋友**测一次，当作 pilot

---

## 4) 数据回收流程

### 文件命名规则（已内置）

原型自动以 Participant ID 命名：
- JSON: `study_data_P01.json`（**推荐收这个**，包含问卷数据）
- CSV: `trial_data_P01.csv`（仅含试次数据）

**你只需要收 JSON 文件**，它包含全部信息（试次 + 问卷 + 配置 + 参与者信息）。

### 回收方式（按省事程度排序）

| 方式 | 操作 | 推荐度 |
|------|------|:---:|
| 微信/iMessage | 参与者直接在手机上下载后发给你 | ⭐⭐⭐ |
| 邮件 | 发到你指定邮箱 | ⭐⭐ |
| Google Drive / 坚果云 | 共享文件夹链接，参与者上传 | ⭐（多一步） |

**给参与者的一句话指引**：
> "做完后点 Download All Data (JSON)，然后把下载的文件发给我就行。"

### 参与者 ID 分配规则

| 参与者到场顺序 | ID | Group |
|:---:|:---:|:---:|
| 第 1 位 | P01 | 1 |
| 第 2 位 | P02 | 2 |
| 第 3 位 | P03 | 3 |
| 第 4 位 | P04 | 1 |
| 第 5 位 | P05 | 2 |
| 第 6 位 | P06 | 3 |
| … | … | 循环 |

你发链接给参与者时，**同时告诉他们 ID 和 Group 编号**。例如：

> "你好，你的编号是 P04，Group 1。打开链接后在 Participant ID 输入 P04，Counterbalance Group 选 Group 1。"

### 额外信息收集

当前原型已收集：
- ✅ Dominant hand（左/右手）
- ✅ Device type（自动检测 mobile/desktop）

不需要额外收集手机型号——`deviceType` 足够区分。如果你特别想知道，可以在发链接时让参与者顺便说一下手机型号，但**不是必须的**。

### 数据完整性检查

收到每个 JSON 文件后，快速验证：
```
打开文件 → 搜索 "trialData" → 确认里面有 72 条记录（3 条件 × 24 试次）
搜索 "questionnaireData" → 确认有 3 个条件的问卷数据
```

如果某人的数据少于 72 条（比如中途退出），记下来即可，分析时排除。

---

## 5) 实验前文档（可直接放进 report 的英文段落）

### 研究问题与假设

> This study investigates how menu radius affects the usability of marking menu input on mobile devices. We compare a conventional direct-tap baseline with two marking menu conditions that differ in radius (24 mm vs 36 mm), hypothesising that the larger radius will improve selection accuracy (due to wider angular sectors) at the cost of slightly increased task time (due to longer finger travel).

### 实验设计

> A within-subjects design was used with one independent variable: **input condition**, consisting of three levels — (A) direct tap baseline, (B1) marking menu with small radius (r = 24 mm), and (B2) marking menu with large radius (r = 36 mm). The dependent variables were task completion time (ms), selection success rate (%), error type distribution, and subjective ratings via the System Usability Scale (SUS), NASA-TLX (raw), and three custom Likert items addressing predictability, cancellation ease, and one-handed comfort.

### Counterbalancing

> To control for learning and fatigue effects, condition presentation order was counterbalanced across participants using a 3 × 3 Latin square. Group 1 completed conditions in the order A → B1 → B2; Group 2 in the order B1 → B2 → A; Group 3 in the order B2 → A → B1. Participants were assigned to groups in rotation based on arrival order. Each condition comprised 8 practice trials followed by 24 formal trials (8 targets × 3 repetitions, randomised with consecutive-duplicate avoidance).

---

## 总结：必须做 vs 可选优化

### ✅ 必须做（你现在就该做的）

| 项目 | 状态 |
|------|------|
| 用 `index_fixed.html` 替换原文件 | 文件已生成 |
| 部署到 GitHub Pages 或 Glitch | 按上面步骤操作 |
| 用自己手机完整跑一遍（pilot test） | 重点测试边缘触发 |
| 准备参与者 ID + Group 分配表 | 上面已提供模板 |
| 把参与指南发给参与者 | 上面已提供文本 |

### 💡 可选优化（不做也不影响实验）

| 项目 | 理由 |
|------|------|
| 把 counterbalance 从 3 组缩为 2 组 | 3 组更严谨，保留原样即可 |
| 删掉 NASA-TLX 只留 SUS | 两个都有更好，问卷不长 |
| 实现真正的角度 hysteresis | 时间不够就不做，sectorCrossings 已经够用 |
| 加手机型号收集字段 | 不是必须，口头问即可 |